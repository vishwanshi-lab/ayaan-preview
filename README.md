# Ayaan & Mehboob Valet Parking — preview

A temporary preview of the rebuilt website, for review only.

Static HTML, CSS and one script, replacing a WordPress install that loaded 48
stylesheets and 27 scripts to render the same five pages.

Blocked from search engines via robots.txt and a noindex tag, so it cannot rank
against the live site. This repository is disposable and will be replaced by the
real deployment once the design is approved.
